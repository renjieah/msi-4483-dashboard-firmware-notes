# Firmware Files

This repository does not commit original firmware, NAND dumps, or large firmware archives.

The hashes below identify the current custom firmware candidates from the private working tree. If binaries are published, they should be attached to GitHub Releases instead of committed into git history.

本仓库不直接提交原厂固件、NAND 镜像或大型固件归档。下面只记录当前候选固件的校验值；如果公开二进制，建议放在 GitHub Release 附件里，不直接进入 git 历史。

